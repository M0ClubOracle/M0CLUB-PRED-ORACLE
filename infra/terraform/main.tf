
terraform {
  required_version = ">= 1.6.0"
}

# This is a scaffold. Replace with your cloud provider resources.
# Typical modules:
# - network (vpc/subnets)
# - k8s cluster (eks/gke/aks)
# - registry (ghcr/ecr)
# - databases (rds/managed postgres)
# - cache (redis)
# - observability (managed prometheus/grafana)
