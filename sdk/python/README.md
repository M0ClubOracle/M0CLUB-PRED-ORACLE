
# m0club (Python)

Install (editable):
```bash
cd sdk/python
python -m pip install -e .
```

Run example (requires API Gateway running on localhost:8080):
```bash
python -m m0club.examples.basic
```
